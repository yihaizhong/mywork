function [ train, test ] = ...
getTrainTestFeatures( allFeatures, labels, classNo, classCount )
%UNTITLED7 此处显示有关此函数的摘要
%   此处显示详细说明
index = find(labels == classNo);
n = length(index);
nindex = randperm(n);
trainIndex = nindex(1:classCount);
testIndex = nindex(classCount + 1:n);
train = allFeatures(index(trainIndex),:);
test = allFeatures(index(testIndex));
end

